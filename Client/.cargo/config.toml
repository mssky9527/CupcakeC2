# Do NOT force a remote replace-with here: this machine often has packages only in
# %USERPROFILE%\.cargo\registry\src\index.crates.io-* (default crates-io layout).
# For China online builds, set user-level mirror or run:
#   powershell -File scripts/cargo-use-rsproxy.ps1
#
# Offline: powershell -File scripts/cargo-use-local-cache.ps1
# then: cargo test --manifest-path core/Cargo.toml --lib --offline

[net]
git-fetch-with-cli = true
